project_id = "zwghpxpuiutccyquyizl"

[functions.challenge-push]
# This endpoint verifies a dedicated, Vault-held dispatch secret itself.
verify_jwt = false

[functions.challenge-push-subscription]
# Auth is verified with auth.getUser (also supports new Supabase API keys).
verify_jwt = false
